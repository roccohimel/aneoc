init:                    // tag
	stf();           // start point
	fso();           // flush stdout

test:                    // tag
	msg("Hello!");   // say hi
	nln();           // make new line
	
	fso();           // flush stdout
	ret(0);          // return

